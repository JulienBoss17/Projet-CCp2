<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style.css">
    <title>Modification de l'utilisateur <%= user.nom %></title>
</head>
<body>
    <style>
        h2 {
    text-align: center;
    padding: 25px;
}
.container a {
    text-decoration: none;
    background-color: black;
    border-radius: 10px;
    padding: 10px;
    color: white;
}
.container {
    width: 1100px;
    padding: 20px;
    border-radius: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    gap: 20px;
    margin: 0 auto;
    margin-top: 25px;
    font-family: Arial, sans-serif;
}

.container h2{
    margin-bottom: 10px;
    font-size: 20px;
    color: #333;
}

.container p{
    font-size: 14px;
    color: #666;
}


.sub-container {
    display: flex;
    flex-direction: column;
    text-align: center;
    gap: 20px;
    margin-top: 15px;
    padding: 15px;
    background: #c1c1c1;
    border-radius: 8px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

.sub-container h3 {
    margin-bottom: 5px;
    font-size: 18px;
    color: #444;
}

.sub-container p {
    font-size: 14px;
    color: #666;
}
</style>
    <%- include('../PARTIALS/navbar') %>
    <div class="container">
        <h1>Modification du livre "<%= book.titre %>"</h1>
        <div class="sub-container">
            <form action="/editbook/<%= book._id %>?_method=PUT" method="POST">
                
                <label for="titre">Titre du livre</label><br>
                <input type="text" id="titre" name="titre" value="<%= book.titre %>"  required>
                <br><br>
            
                <label for="auteur">Auteur</label><br>
                <input id="auteur" name="auteur" rows="4" cols="50" value="<%= book.auteur %>"required>
                <br><br>
    
                <label for="year">Année de publication</label><br>
                <input type="date"  id="year" name="year" rows="4" cols="50" value="<%= new Date().toISOString().split('T')[0] %>" required>
                <br><br>
            
                <input type="submit" value="Modifier le livre">
            </form>
        </div>
        <a href="/compte">Revenir au compte</a> 
    </div>
</body>
</html>
