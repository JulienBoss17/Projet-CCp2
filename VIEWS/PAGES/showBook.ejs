<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <style>
        h2 {
            text-align: center;
            padding: 25px;
        }

        .container {
            width: 900px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            gap: 20px;
            margin: 0 auto;
            margin-top: 25px;
            font-family: Arial, sans-serif;
        }

        .container h2{
            margin-bottom: 10px;
            font-size: 20px;
            color: #333;
        }
        
        .container p{
            font-size: 14px;
            color: #666;
        }
        
        .container button:hover{
            background-color: #3e8e41;
        }
    </style>
    <%- include('../PARTIALS/navbar') %>
        <h2>Mes livres :</h2>
        <% books.forEach(book => { %>
            <div class="container">
                <h3>Titre : <%= book.titre %></h3>
                <p>Auteur : <%= book.auteur %></p>
                <p>Année de sortie : <%= new Date(book.year).toLocaleDateString("fr-FR") %></p>
                <a href="/updatebook/<%= book._id %>">Modifier ce livre</a>
            </div>
        <% }); %>
    <%- include('../PARTIALS/footer') %>
</body>
</html>