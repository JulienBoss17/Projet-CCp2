<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="../style.css">
    <title>Suppresion de l'utilisateur <%= user.nom %></title>
</head>
<body>
    <style>
                       h2 {
            text-align: center;
            padding: 25px;
        }
        .container a {
            text-decoration: none;
            background-color: black;
            border-radius: 10px;
            padding: 10px;
            color: white;
        }
        .container {
            width: 1100px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            gap: 20px;
            margin: 0 auto;
            margin-top: 25px;
            margin-bottom: 25px;
            font-family: Arial, sans-serif;
        }

        .container h2{
            margin-bottom: 10px;
            font-size: 20px;
            color: #333;
        }
        
        .container p{
            font-size: 14px;
            color: #666;
        }

        .sub-container {
            display: flex;
            flex-direction: column;
            text-align: center;
            gap: 20px;
            margin-top: 15px;
            padding: 15px;
            background: #c1c1c1;
            border-radius: 8px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        }

        .sub-container h3 {
            margin-bottom: 5px;
            font-size: 18px;
            color: #444;
        }
        
        .sub-container p {
            font-size: 14px;
            color: #666;
        }
    </style>
    <%- include('../PARTIALS/navbar') %>
    <div class="container">
        <h1>Supprimmer son compte</h1>
        <p>Voulez vous vraiment supprimer votre compte?</p>
        <p><strong>Nom du compte :</strong> <%= user.nom %></p>
        <form action="/deleteuser/<%= user._id %>?_method=DELETE" method="POST">
            <button type="submit">Oui, Supprimer</button>
        </form>
        <a href="/compte">Revenir en arrière</a>
    </div>
</body>
</html>